<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialComponents.DayNight.NoActionBar"
        android:usesCleartextTraffic="true">

        <!-- ==================== ACTIVITIES ==================== -->

        <!-- LoginActivity - Màn hình khởi động -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".RegisterActivity" android:exported="false" />
        <activity android:name=".HomeActivity" android:exported="false" />
        <activity android:name=".ProfileActivity" android:exported="false" />
        <activity android:name=".AIChefActivity" android:exported="false" />
        <activity android:name=".RecipeDetailActivity" android:exported="false" />
        <activity android:name=".CookbookActivity" android:exported="false" />
        <activity android:name=".OrderListActivity" android:exported="false" />
        <activity android:name=".OrderTrackingActivity" android:exported="false" />
        <activity android:name=".ChatActivity" android:exported="false" />
        <activity android:name=".SearchActivity" android:exported="false" />
        <activity android:name=".SelectShopperActivity" android:exported="false" />
        <activity android:name=".ShopperMapActivity" android:exported="false" />
        <activity android:name=".WalletActivity" android:exported="false" />

        <!-- Đi chợ hộ - New activities -->
        <activity android:name=".CreateShoppingRequestActivity" android:exported="false" />
        <activity android:name=".ShopperDashboardActivity" android:exported="false" />
        <activity android:name=".ShopperShoppingActivity" android:exported="false" />
        <activity android:name=".OrderWaitingActivity" android:exported="false" />
        <activity android:name=".OrderChatActivity" android:exported="false" />
        <activity android:name=".ConversationListActivity" android:exported="false" />

        <!-- Chợ đồng hương - Community activities -->
        <activity android:name=".CommunityFeedActivity" android:exported="false" />
        <activity android:name=".CreatePostActivity" android:exported="false" />
        <activity android:name=".PostDetailActivity" android:exported="false" />



        <!-- ==================== SERVICES ==================== -->

        <!-- LocationService - Foreground Service lấy vị trí GPS -->
        <service
            android:name=".service.LocationService"
            android:foregroundServiceType="location"
            android:exported="false" />

        <!-- OrderPollingService - Theo dõi trạng thái đơn hàng -->
        <service
            android:name=".service.OrderPollingService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />

        <!-- ==================== BROADCAST RECEIVERS ==================== -->

        <!-- NetworkStateReceiver - Lắng nghe thay đổi kết nối mạng -->
        <receiver
            android:name=".receiver.NetworkStateReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
            </intent-filter>
        </receiver>

        <!-- OrderStatusReceiver - Đăng ký động trong OrderTrackingActivity -->

        <!-- ==================== CONTENT PROVIDER ==================== -->

        <!-- RecipeContentProvider - Chia sẻ dữ liệu công thức nấu ăn -->
        <provider
            android:name=".provider.RecipeContentProvider"
            android:authorities="com.gomarket.provider"
            android:exported="false" />

    </application>

</manifest>
