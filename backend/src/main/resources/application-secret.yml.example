# === TEMPLATE - Copy file này thành application-secret.yml ===
# cp application-secret.yml.example application-secret.yml
# Sau đó điền key thật vào bên dưới

api:
  openweather:
    key: YOUR_OPENWEATHER_API_KEY_HERE

  openrouter:
    key: YOUR_OPENROUTER_API_KEY_HERE
